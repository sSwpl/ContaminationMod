package com.example.contamination.data;

/**
 * Data generator disabled: original implementation caused build failures under current NeoForge/mappings.
 * Keeping a small stub here so the project compiles. If you want the generator, reintroduce a RecipeProvider
 * implementation compatible with your mappings (constructor signatures and types vary by Minecraft/Neoforge version).
 */
public final class ModRecipeProvider {
    private ModRecipeProvider() {}
}